# v0.26.0 — 本地优先工作区重定位

> 2026-08-28 · 基于 v0.25.1 的 4 个提交

## ✨ Highlights

### 本地优先定位重塑

此前定位为轻量 SSH 客户端（`~/.ssh/config` 单一真相源），本版本重定位为本地优先终端工作区（`src/main.rs:8`, `README.md:3`）。

- **README/CLI/打包**：slogan 改为 `本地优先终端工作环境（GPUI）`，Features 重排为 `LocalSession/LocalDir` / `crossh-git` / `crossh-note` 置顶，SSH/SFTP/转发降级为 Remote 能力；`crossh --help` header 同步为 `Crossh {ver} — Local-first terminal workspace (GPUI)`，新增 `note` 子命令（`src/app/cli.rs:18` / `src/main.rs:20`），AUR `PKGBUILD/.SRCINFO` 与 `scripts/package-linux.sh:41` 的 Desktop Comment 同步
- **本地化**：`locales/zh-CN.yml:12` / `en.yml` 的 `about_description` / `search_placeholder` 改为过滤项目/主机
- **文档**：`crossh-agent` 已于 `5daa8ce` 删除，`Runtime/EventBus` 已移除，分层说明已同步至 `docs/architecture.md`

## 💅 Improvements

### Workspace 清理（SSH 中心化残留移除）

- `empty_state.rs:24` 移除 `Hosts` 过滤与 host continue 条目，仅保留本地流（`EmptyStateFilter/ContinueEntry`）；`sidebar.rs:188` 移除 Active/Bank 主机分组、host 渲染与 rail 主机聚合（`render_sidebar` / `rail_add_menu_entries`）；`shell.rs:18` 将 `host_query` 重命名为 `search_query`，`open_query` 简化为纯本地，移除回退 SSH 直连与 bank/active 切换
- 搜索占位与空状态文案改为 local-first，`Local` 为主入口，`Active/Bank` 隐藏；`HostEntry::detail/matches_query` 保留 `#[allow(dead_code)]` 供后续远程 UI 复用

## 🔧 Chores

- **CI**：移除 `ci.yml` 的 `fmt/clippy/test/terminal-compat`，仅保留 `release.yml` 按 `v*` tag 触发（`9dfd461`）
- **协作规范**：`AGENTS.md:18` 新增 Test Execution Discipline，默认避免 `cargo test --workspace`，仅定向测试、发布前才全量（与简化审计硬门禁对齐）

## 📦 升级说明

无需迁移。SSH 能力保留为可插拔 `WorkspacePane`，`~/.ssh/config` 只读解析不变；`note.db` / `settings.toml` 路径不变；回退至 `v0.25.1` 直接替换二进制即可。
