# v0.34.0 — 状态栏 Git 实时化与分栏拖拽

> 2026-09-11 · 基于 v0.33.0 的 8 个提交

## ✨ Highlights

### 状态栏 Git：后台 fetch + 短 hash

- 聚焦会话约每 5 分钟后台 `git fetch --all`，behind 计数自动跟上远端；push/pull 在途或上次 fetch 未完成时跳过，失败只记日志不打扰（`src/features/workspace/shell/mod.rs`，`7b69cec`）
- 分支名旁显示 HEAD 7 位短 hash，detached 不再挤进分支名，空仓库不显示（`crates/crossh-core/src/git_status.rs`，`src/features/git/render.rs`，`767ce53`）

### 分栏交叉点双向拖拽

- 竖线与横线交叉处新增双向拖拽热区，一次手势同时调整宽高，配 move 光标（`src/features/workspace/view.rs`，`c28d23d`）

### 内置更新下载进度

- 设置页更新下载从"转圈"变为进度条 + 已下载/总量字节数，120ms 轮询刷新（`src/features/updates/mod.rs`，`crates/crossh-update/src/client.rs`，`03abd4d`）

## 🛠 Fixes

- debug 构建单实例端口隔离，debug 版与正式版可并存运行（`src/app/single_instance.rs`，`105781f`）

## 🔧 Chores

- Zed 同步至 `2518540`（09-11，领先 80 提交，`c0f72a1`）
- vendor ponytail skill（pin `356918e`，AGENTS 默认 full）与架构检查误报修复（`8e5fbd4`，`f55f6ea`）

## 📦 升级说明

无需手动迁移。其他平台可直接内置更新。
