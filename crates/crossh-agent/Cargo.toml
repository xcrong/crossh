[package]
name = "crossh-agent"
version = "0.14.4"
edition = "2024"
license = "GPL-3.0-or-later"

[dependencies]
crossh-ai-sdk = { path = "../crossh-ai-sdk" }
libc = "0.2"
regex = "1"
dirs = "6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
